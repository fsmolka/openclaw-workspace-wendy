# Health Algo

Public demo shell for the Health Algo dashboard.

This deployment intentionally uses demo data. Private Health Auto Export payloads, tokens, receiver URLs, and real daily rollups stay outside the public app until an authenticated data path is added.
